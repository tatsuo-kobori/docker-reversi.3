# リバーシ（ミニオセロ）プログラム設計書

## 1. 概要

本プログラムは、ブラウザ上で動作するリアルタイム対戦型リバーシ（オセロ）ゲームである。
3つの固定ルームを持ち、各ルーム内で「勝ち抜き（勝者残留）方式」の対戦を行う。
Socket.IO によるリアルタイム通信で、ルーム管理・盤面の状態・駒の配置・勝敗判定をサーバー側で一元的に管理する。

- 対戦人数：1 ルームにつき 2 人（白・黒）
- ルーム：固定3つ（ユーザーが参加・観戦できる）
- 盤面：8×8
- 通信：Socket.IO（WebSocket / ポーリング）

### 勝ち抜き方式

各ルームは「エントリーリスト（順序付き）」を持ち、リストの上位 2 名が対戦する。

- リスト最上位（1 番目）＝ 白、2 番目 ＝ 黒、3 番目以降 ＝ 待機（観戦）
- 対戦決着後、**勝者はリスト最上位（白）に残留**、敗者はリスト最後尾へ移動
- リストの次点が繰り上がって黒（挑戦者）になり、自動的に対戦開始

## 2. 技術スタック

| 区分 | 技術 |
|---|---|
| フロントエンド | Vue 3 / TypeScript / Vite |
| UI フレームワーク | Vuetify 3 / Bootstrap 5（グリッド・ユーティリティ） |
| 状態管理 | Pinia |
| リアルタイム通信 | Socket.IO（クライアント: socket.io-client） |
| バックエンド | Node.js / Express / TypeScript |
| リアルタイムサーバー | Socket.IO（socket.io） |
| 設定管理 | node-config |

## 3. システム構成

```
[ブラウザ (クライアント)]
   Vue 3 + Vuetify + Bootstrap
        │  Socket.IO (WebSocket)
        ▼
[Socket.IO サーバー (Node.js + Express)]
   - ルーム管理（複数ルーム・エントリーリスト）
   - ゲーム状態管理（盤面・手番）
   - リバーシロジック（探索・反転・勝敗判定）
        │
        ▼
[nginx（本番環境）]
   - 静的ファイル配信（dist）
   - /socket.io/ をバックエンドへプロキシ
```

### ディレクトリ構成

```
reversi.docker.3/
├─ docker-compose.yml
├─ reversi.client/          # フロントエンド
│  ├─ src/
│  │  ├─ components/        # 画面コンポーネント
│  │  ├─ stores/            # Pinia ストア
│  │  ├─ types/             # 型定義
│  │  ├─ config/            # 接続先設定
│  │  ├─ plugins/           # Vuetify / Pinia / Router
│  │  ├─ App.vue            # メインコンポーネント
│  │  └─ main.ts            # エントリポイント
│  ├─ .env.development      # 開発用環境変数
│  ├─ .env.production       # 本番用環境変数
│  └─ vite.config.mts
└─ reversi.server/          # バックエンド
   ├─ src/
   │  ├─ logic/             # リバーシロジック
   │  ├─ types/             # 型定義
   │  ├─ app.ts             # サーバー起動
   │  └─ socket.ts          # Socket.IO イベント処理
   └─ config/               # サーバー設定
```

## 4. 電文仕様（Socket.IO 通信）

### 4.0 概要

クライアントとサーバーは Socket.IO で通信する。ペイロードは原則 **JSON 文字列**（`JSON.stringify` したもの）で送受信する。
一部のイベントは文字列（`gameStart` / `gameOver`）や空文字（`pass` / `surrender`）で送信する。

盤面・エントリー・対戦に関するイベント（`entryInfo` / `moveInfo` / `gameStart` / `gameOver`）は、
**ルーム単位（`io.to(roomId)`）で配信**する。ルーム一覧（`roomList`）は全クライアントへ配信する。

### 4.1 クライアント → サーバー

#### 4.1.1 joinRoom（ルーム参加・観戦）

既存ルームに参加して観戦する。参加した時点ではエントリーリストには加わらない（観戦モード）。

| フィールド | 型 | 説明 |
|---|---|---|
| `roomId` | string | 参加するルームの ID |

```json
{ "roomId": "room-1" }
```

#### 4.1.2 entry（エントリー要求）

ハンドル名を入力して、現在いるルームのエントリーリストに参加する。

| フィールド | 型 | 説明 |
|---|---|---|
| `name` | string | ハンドル名（最大10文字） |
| `detail` | string | 詳細（未使用、空文字） |
| `mode` | string | 色。クライアントでは空文字を送信し、サーバーで割り当てる |
| `socketId` | string | 接続ID。クライアントでは空文字を送信し、サーバーで設定する |

```json
{
  "name": "tat",
  "detail": "",
  "mode": "",
  "socketId": ""
}
```

#### 4.1.3 move（駒を置く）

駒を置く際に送信する。

| フィールド | 型 | 説明 |
|---|---|---|
| `color` | string | 置く駒の色（`"B"` / `"W"`） |
| `position` | Position | 置く位置 |
| `position.x` | number | 列（0〜7） |
| `position.y` | number | 行（0〜7） |

```json
{
  "color": "B",
  "position": { "x": 2, "y": 3 }
}
```

#### 4.1.4 pass（パス）

自分の手番でパスする際に送信する。ペイロードは空文字（`""`）。

#### 4.1.5 surrender（降参）

降参する際に送信する。ペイロードは空文字（`""`）。

#### 4.1.6 exit（退室）

現在のルームから退室する際に送信する。ペイロードなし。

### 4.2 サーバー → クライアント

#### 4.2.1 roomList（ルーム一覧）

ルームの一覧を配信する。接続時および各ルームのエントリーリスト変更時に全クライアントへ送信する。

| フィールド | 型 | 説明 |
|---|---|---|
| `rooms` | RoomInfo[] | ルームの一覧 |
| `rooms[].roomId` | string | ルーム ID |
| `rooms[].name` | string | ルーム名（表示用） |
| `rooms[].disabled` | boolean | 利用可否（true で利用不可） |
| `rooms[].entryUsers` | EntryUserInfo[] | そのルームのエントリーリスト（順序付き） |

```json
{
  "rooms": [
    {
      "roomId": "room-1",
      "name": "ルーム 1",
      "disabled": false,
      "entryUsers": [
        { "name": "Alice", "detail": "", "mode": "W", "socketId": "abc123" },
        { "name": "Bob",   "detail": "", "mode": "B", "socketId": "def456" }
      ]
    }
  ]
}
```

#### 4.2.2 roomJoined（ルーム入室通知）

ルーム参加（`joinRoom`）が成功した際に、そのクライアントへ自分のルーム ID を通知する。

| フィールド | 型 | 説明 |
|---|---|---|
| `roomId` | string | 入室したルームの ID |

```json
{ "roomId": "room-1" }
```

#### 4.2.3 entryAccept（エントリー受付）

`entry` を送信した本人に対して、受付完了を通知する。

| フィールド | 型 | 説明 |
|---|---|---|
| `status` | string | 受付結果（`"OK"`） |

```json
{ "status": "OK" }
```

#### 4.2.4 entryInfo（ルーム内エントリー一覧の更新）

現在のルームのエントリーリストの変更を、そのルーム内の全員に配信する。

| フィールド | 型 | 説明 |
|---|---|---|
| `users` | EntryUserInfo[] | エントリーリスト（順序付き） |
| `users[].name` | string | ハンドル名 |
| `users[].detail` | string | 詳細（未使用） |
| `users[].mode` | string | 色（`"B"`=黒 / `"W"`=白 / `"E"`=待機・観戦） |
| `users[].socketId` | string | 接続ID |

```json
{
  "users": [
    { "name": "Alice", "detail": "", "mode": "W", "socketId": "abc123" },
    { "name": "Bob",   "detail": "", "mode": "B", "socketId": "def456" }
  ]
}
```

#### 4.2.5 moveInfo（盤面の更新）

盤面の状態を、そのルーム内の全員に配信する。

| フィールド | 型 | 説明 |
|---|---|---|
| `board` | string[][] | 8×8 の盤面（各セルは `"B"` / `"W"` / `""`） |
| `invertedPositions` | Position[] | 直近で反転した駒の位置 |
| `nextTurn` | string | 次の手番（`"B"` / `"W"`） |
| `placesToPut` | Position[] | 置ける位置の一覧 |

```json
{
  "board": [
    ["","","","","","","",""],
    ["","","","","","","",""],
    ["","","","","","","",""],
    ["","","","W","B","","",""],
    ["","","","B","W","","",""],
    ["","","","","","","",""],
    ["","","","","","","",""],
    ["","","","","","","",""]
  ],
  "invertedPositions": [],
  "nextTurn": "B",
  "placesToPut": [
    { "x": 3, "y": 2 },
    { "x": 2, "y": 3 },
    { "x": 5, "y": 4 },
    { "x": 4, "y": 5 }
  ]
}
```

#### 4.2.6 gameStart（対戦開始）

白・黒が揃って対戦が開始される際に、対戦者2名へ自色を通知する。ペイロードは文字列。

| 値 | 説明 |
|---|---|
| `"W"` | 自分が白番 |
| `"B"` | 自分が黒番 |

#### 4.2.7 gameOver（勝敗通知）

対戦終了時に、そのルーム内の全員へ勝敗を通知する。ペイロードは文字列。

| 値 | 説明 |
|---|---|
| `"W"` | 白の勝ち |
| `"B"` | 黒の勝ち |
| `"-"` | 引き分け |

## 5. データ構造（型定義）

### 5.1 Position
```ts
interface Position {
  x: number;  // 列（0〜7）
  y: number;  // 行（0〜7）
}
```

### 5.2 BoardInfo
```ts
interface BoardInfo {
  board: string[][];            // 8×8 の盤面（"B" / "W" / ""）
  invertedPositions: Position[]; // 直近で反転した駒の位置
  nextTurn: string;              // 次の手番（"B" / "W"）
  placesToPut: Position[];       // 置ける位置の一覧
}
```

### 5.3 EntryUserInfo
```ts
interface EntryUserInfo {
  name: string;      // ハンドル名
  detail: string;    // 詳細（未使用）
  mode: string;      // "B"（黒）/ "W"（白）/ "E"（待機・観戦）
  socketId: string;  // Socket.IO の接続 ID
}
```

### 5.4 EntryUsersList
```ts
interface EntryUsersList {
  users: EntryUserInfo[];
}
```

### 5.5 RoomInfo（ルーム情報）
```ts
interface RoomInfo {
  roomId: string;             // ルーム ID
  name: string;               // ルーム名（表示用）
  disabled: boolean;          // 利用可否（true で利用不可）
  entryUsers: EntryUserInfo[]; // エントリーリスト（順序付き）
}
```

### 5.6 RoomList（ルーム一覧）
```ts
interface RoomList {
  rooms: RoomInfo[];
}
```

### 5.7 MoveInfo
```ts
interface MoveInfo {
  color: string;     // 置く駒の色（"B" / "W"）
  position: Position; // 置く位置
}
```

### 5.8 Players
```ts
interface Players {
  whiteSeat: string;  // 白番ユーザーの socketId
  blackSeat: string;  // 黒番ユーザーの socketId
}
```

### 5.9 GameResult
```ts
interface GameResult {
  winnerId: string;
  loserId: string;
}
```

## 6. サーバー側設計

### 6.1 app.ts（サーバー起動）

- Express で HTTP サーバーを生成し、Socket.IO サーバーを起動する。
- CORS 設定（`corsOrigin`）を `config` から読み込む。
- ポート `4100`、ホスト `0.0.0.0` で待ち受ける。

### 6.2 socket.ts（イベント処理・ルーム管理）

サーバー内で保持する状態：

| 変数 | 型 | 説明 |
|---|---|---|
| `rooms` | `Map<string, RoomState>` | ルーム ID → ルーム状態 |

ルーム状態（`RoomState`）：

| フィールド | 型 | 説明 |
|---|---|---|
| `roomId` | string | ルーム ID |
| `name` | string | ルーム名（表示用） |
| `disabled` | boolean | 利用可否（true で利用不可） |
| `boardInfo` | BoardInfo | 盤面・手番・置ける位置 |
| `entryList` | EntryUserInfo[] | エントリーリスト（順序付き。先頭=白、2番目=黒） |

各ソケットは `socket.data.roomId` に現在のルーム ID を保持する。

主要関数：

| 関数 | 説明 |
|---|---|
| `boardInit` | 盤面を初期化（中央に白2・黒2） |
| `gameStart` | 盤面初期化、手番を黒に設定 |
| `judgement` | 勝敗判定（"W" / "B" / "-" / ""） |
| `assignModes` | エントリーリストの順序に基づき色を割り当て（2人以上で先頭=W、2番目=B、以降=E。1人だけならE） |
| `rotateAfterGame` | 対戦決着後、勝者を先頭・敗者を最後尾へ並べ替える |
| `createRoomState` | ルーム状態を初期化して返す（`config` の `rooms` 配列から生成） |

### 6.3 ReversiLogic.ts（ゲームロジック）

| 関数 | 説明 |
|---|---|
| `searchBoard` | 指定方向へ再帰的に探索し、挟んだ駒を反転 |
| `searchPlaceToPut` | 全空きマスについて8方向を探索し、置ける位置を列挙 |

## 7. クライアント側設計

### 7.1 App.vue（メインコンポーネント）

- Socket.IO 接続を確立し、サーバーイベントを受信して Pinia ストアへ反映する。
- 接続時にルーム一覧を取得し、ルーム一覧ポップアップを表示する。
- ハンバーガーメニュー（`v-navigation-drawer`）でルーム一覧 / エントリー / 退室を提供。
- メインコンテンツを Bootstrap のグリッド（`container` / `row` / `col`）でレイアウト。

主要関数：

| 関数 | 説明 |
|---|---|
| `isEntry` | 自分がエントリー済みか判定 |
| `mode` | 自分の色（"B" / "W" / ""）を取得 |
| `currentTurn` | 現在の手番を取得 |
| `onMove` | 駒を置く（`move` を送信） |
| `onJoinRoom` | ルームに参加（`joinRoom` を送信） |
| `onEntry` | エントリー（`entry` を送信） |
| `onExit` / `onPass` / `onGiveUp` | 退室 / パス / 降参 |
| `onShowRoomList` | ルーム一覧ポップアップ表示 |

### 7.2 Pinia ストア（GameBoardStore）

| 状態 | 説明 |
|---|---|
| `boardInfo` | 盤面情報 |
| `entryUsersList` | 現在のルームのエントリーリスト |
| `roomList` | ルーム一覧 |
| `currentRoomId` | 現在のルーム ID |
| `turnInfo` | 手番情報 |

アクション：`setBoardInfo` / `setEntryUsersList` / `setRoomList` / `setTurn` / `pieceColor`

### 7.3 コンポーネント一覧

| コンポーネント | 役割 |
|---|---|
| `GameBoard` | 8×8 の盤面を CSS Grid で描画、クリックで `move` を発火 |
| `GameSquare` | 1 マスの駒を描画 |
| `GameMatchTitle` | 対戦タイトル（VS / 待機中）を表示 |
| `GameInformation` | 手番（自分のターン / 相手のターン / 観戦）を表示 |
| `GameController` | パス / まいったボタン |
| `GameRoomListDialog` | ルーム一覧ポップアップ（各ルームのエントリー者を表示） |
| `GameEntryDialog` | エントリー（ハンドル入力）ダイアログ |
| `GameEntryUsersDialog` | ルーム内エントリー一覧ポップアップ（黒・白マーク付き） |
| `GameHowToDialog` | あそび方ポップアップ |
| `GameSplash` | ゲーム開始 / 勝敗のスプラッシュ表示 |

## 8. 画面設計

### 8.1 メイン画面

```
┌─────────────────────────────────────┐
│  [REVERSI ロゴ]            [≡ メニュー] │  ← v-app-bar
├─────────────────────────────────────┤
│                                     │
│      [ 対戦タイトル / 待機中表示 ]      │  ← GameMatchTitle
│                                     │
│      ┌───────────────────────┐      │
│      │   8×8 盤面 (CSS Grid)   │      │  ← GameBoard
│      └───────────────────────┘      │
│                                     │
│      [ 手番情報（ターン表示） ]        │  ← GameInformation
│                                     │
├─────────────────────────────────────┤
│      [ パス！ ] [ まいった！ ]         │  ← v-footer（自分の手番のみ表示）
└─────────────────────────────────────┘
                        [あそび方]      ← 右下固定ボタン
```

### 8.2 ルーム一覧ポップアップ

画面アクセス時およびハンバーガーメニューから表示する。固定3ルームと各ルームのエントリー者一覧を表示し、ルームの選択（観戦）ができる。

### 8.3 モード

| モード | 状態 | できること |
|---|---|---|
| 観戦モード | ルーム参加直後（名前未入力） | 対戦の鑑賞、エントリー一覧の閲覧 |
| エントリーモード | エントリー済み（名前入力済み） | 対戦参加（リスト上位2名）、観戦 |

- ハンバーガーメニュー：ルーム一覧 / エントリー / エントリー一覧 / 退室
- レスポンシブ対応：盤面は `width: 100%` + `aspect-ratio: 1/1` で画面幅に追従し、常に正方形を維持

## 9. ゲームロジック

### 9.1 初期配置

盤面中央に白2・黒2を配置：

```
  3   4
3  W   B
4  B   W
```

初期手番は黒（`nextTurn = "B"`）。

### 9.2 色の割り当て（エントリーリスト順）

エントリーリストは順序付きで、先頭から順に色を割り当てる：

| 位置 | 色 | 役割 |
|---|---|---|
| 1 番目（最上位） | 白（W） | チャンピオン |
| 2 番目 | 黒（B） | 挑戦者 |
| 3 番目以降 | 待機（E） | 観戦 |

エントリーリストが 1 人だけの場合は対戦にならないため、そのユーザーは待機（E）となる。

### 9.3 駒を置く処理

1. クライアントが `move`（色・位置）を送信。
2. サーバーは位置が `placesToPut` に含まれ、かつ `nextTurn` と色が一致することを検証。
3. 盤面に駒を置き、8 方向（N/NE/E/SE/S/SW/W/NW）について `searchBoard` で挟んだ駒を反転。
4. `judgement` で勝敗判定。
   - 継続：手番を交代し、`searchPlaceToPut` で置ける位置を再計算。
   - 終了：`gameOver` をルーム内に配信し、勝ち抜きの並べ替えを行う。

### 9.4 勝敗判定（judgement）

以下のいずれかに該当した場合に終了：

- 盤面が全て埋まる（64 個）
- 白または黒が 0 個
- 両者とも置ける位置が 0

勝敗は駒の多い方の勝ち、同数なら引き分け（`"-"`）。

### 9.5 勝ち抜きの並べ替え（rotateAfterGame）

対戦決着後、エントリーリストを以下のように並べ替える：

- **白が勝った場合**：白（先頭）はそのまま、黒（敗者）をリスト最後尾へ移動
- **黒が勝った場合**：黒が先頭（白）に昇格、元の白（敗者）をリスト最後尾へ移動

並べ替え後、`assignModes` で色を再割り当てし、リストが 2 人以上なら自動的に対戦を開始する。

## 10. 処理フロー

### 10.1 接続 → ルーム一覧

1. クライアントが接続。
2. サーバーが `roomList`（ルーム一覧）を配信。
3. クライアントがルーム一覧ポップアップを表示。

### 10.2 ルーム参加 → 観戦

1. ユーザーがルームを選択。
2. クライアントが `joinRoom` を送信。
3. サーバーがソケットをルームに参加させ、入室通知（`roomJoined`）で自分のルーム ID を通知し、盤面（`moveInfo`）とエントリーリスト（`entryInfo`）を送信。
4. ユーザーは観戦モード（名前未入力）で対戦を鑑賞できる。

### 10.3 エントリー → 対戦開始

1. ユーザーがハンドル名を入力し `entry` を送信。
2. サーバーがエントリーリストに追加し、`assignModes` で色を割り当て。
3. `entryInfo` をルーム内に配信。
4. リストが 2 人以上なら、上位 2 名で対戦開始（`gameStart` を両者へ配信）。

### 10.4 駒を置く → 盤面更新

1. `move` 受信 → 検証 → 駒配置 → 反転 → 勝敗判定。
2. `moveInfo` をルーム内に配信（継続時は手番交代）。

### 10.5 対戦決着 → 勝ち抜き

1. `gameOver` をルーム内に配信。
2. `rotateAfterGame` で勝者を先頭・敗者を最後尾へ並べ替え。
3. `assignModes` で色を再割り当て。
4. リストが 2 人以上なら自動的に対戦開始。

### 10.6 パス / 降参 / 退室

- **パス**：自分の手番のときのみ、手番を交代。
- **降参**：エントリーリストの状態を更新し、勝ち抜きの並べ替えを行う。
- **退室 / 切断**：エントリーリストから削除し、ルームを離脱。ルームが空になったら盤面を初期化して待機状態に戻す。

## 11. 設定・環境

### 11.1 クライアント環境変数（Vite）

| ファイル | 内容 |
|---|---|
| `.env.development` | `VITE_SOCKET_URL=http://localhost:4100` |
| `.env.production` | （未設定 = 同一オリジン接続） |

接続先は `src/config/default.ts` の `SOCKET_URL` で解決され、未設定時は同一オリジン（nginx のプロキシ経由）へ接続する。

### 11.2 サーバー設定（config/default.js）

| 項目 | 値 | 説明 |
|---|---|---|
| `corsOrigin` | `["http://localhost:3000", "https://reversi.hoge9.xyz"]` | 許可するオリジン（開発・本番） |
| `port` | `4100` | 待ち受けポート |
| `host` | `0.0.0.0` | 待ち受けホスト |
| `timeout` | `10000` | タイムアウト（ms） |
| `rooms` | `[{ name, disabled }, ...]` | ルーム定義（JSON 配列）。起動時にここからルームを生成（roomId は配列順から採番） |

### 11.3 ポート

| サービス | ポート |
|---|---|
| クライアント（Vite dev） | 3000 |
| サーバー（Socket.IO） | 4100 |
| Docker（クライアント） | 4000 → 3000 |

## 12. 既知の課題・注意点

- `GameResult` 型は定義されているが、実際の `gameOver` では勝者文字列（"W"/"B"/"-"）を直接送信している。
- 未使用のスキャフォールド（`AppFooter.vue` / `GameMessageArea.vue` / `HelloWorld.vue` 等）が残っている。
- ルームごとのチャット機能は将来フェーズとして検討中（本設計には未反映）。
