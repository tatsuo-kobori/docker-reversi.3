# 本番ビルド用（npm run build）
#
# nginx が /socket.io/ をバックエンド(4100)へプロキシしているため、
# クライアントは同一オリジン（https://reversi.hoge9.xyz/socket.io/）へ接続する。
# VITE_SOCKET_URL は設定しない（未設定 = 同一オリジン接続）。
#
# 明示的に接続先を指定したい場合は以下を有効化する：
# VITE_SOCKET_URL=https://reversi.hoge9.xyz
